---
title: Brother Developer Agent — audit evidence and the P0 queue
domain: developer
repo: sabuj14eu/brother-developer
sources: docs/JOB3_ISOLATION_2026-09-04.md, docs/JOB8_9_TRADING_LOGIC_2026-09-05.md, docs/P0_RELEASE_REPORT_2026-09-05.md, docs/WINDOW2_ISO02_ISO09_2026-09-05.md, docs/HEARTBEAT_WORK_ORDER_2026-09-05.md, brother_developer/memory/bugs/, brother_developer/ledger/ledger.jsonl, tests/audit/2026-09-04_job3/test_cross_arm_isolation.py
verified_on: 2026-09-16
classification: INTERNAL
---

# The Brother Developer audit in one paragraph

Between 2026-09-04 and 2026-09-05 the bot-side Brother Developer windows rebuilt the ten dual-MT5 isolation tests as fixtures (Job 3), reviewed the trading logic for fallbacks and model-computed values (Jobs 8 and 9), found eleven P0 defects (ISO-01, 02, 03, 05, 09, 10, 12, 14, 16, 19, 24), and by the end of 2026-09-05 had one resolved on the box (ISO-01, deployed by Shyam) and ten fixed in the trading repositories on branch `claude/sniper-isolation-audit-completion-xdj6z4`, awaiting the release gate. The final release report (`docs/P0_RELEASE_REPORT_2026-09-05.md`, commit `55aba5b`, ledger row 39) counts 296 tests passed, 0 failed across the four suites and gives REAL MONEY a NO-GO.

# Why the audit started from zero: the missing n45mwm audit

`docs/JOB3_ISOLATION_2026-09-04.md` §0: the task said to read `docs/AUDIT_ACCOUNT_ISOLATION_2026-09-04.md` on branch `claude/sniper-account-isolation-audit-n45mwm`. It was searched in every remote ref of all four repos, `git log --all`, GitHub code search and local disk, and does not exist anywhere reachable — that window "could not push" and its 661 lines of findings "existed in one ephemeral container" (`docs/SESSION_PROTOCOL.md`). Verdict on "read the audit": NOT RUNNABLE (ledger row 2, `audit_missing`). Everything after was derived from the branch heads v7 `c1618f5` and brain `cfc52e9`.

# The P0 queue, finding by finding

Each entry: what was found (with pre-fix file:line), the fix and where it lives, tests, and box status as of 2026-09-05. Sources: the memory record `brother_developer/memory/bugs/BUG-…-ISO<nn>-….json`, `docs/P0_RELEASE_REPORT_2026-09-05.md` §1 and §8, ledger rows 29–39.

**ISO-01 — v7 bridge executes on whatever account the terminal holds (brother_sniper_v7, ADR-004).** `sniper_executor.py:21` `V7_MT5_PATH` was the only "identity"; `:64` and `:76` called `mt5.initialize(path=…)` without login; `:68-72`/`:79-83` only logged `acc.login`; `core/ic_markets.py:20-32` set `_login_ok=True` on any HTTP 200. Reproduced: a POST `/execute` lands on the foreign account and returns `status ok`. Fix: `V7_MT5_LOGIN` required from the environment, `_identity_ok(acc)` on every request and after every reconnect, 503 on mismatch or missing env (14 added lines, one file, wire contract untouched). Deployed by Shyam on the Windows box 2026-09-05 00:08:15 box time via `patch_iso01_identity.py` (backup `sniper_executor.py.bak.20260905_000815`, NSSM env `V7_MT5_LOGIN=52834417`, restart 00:09). Verified on three witnesses (`/health` 200 with account 52834417; bot secret still accepted; wrong secret rejected) and then by the direct witness — marker lines 259–286 in the running `C:\Users\Administrator\sniper_executor.py`. Status: **resolved** (ledger rows 10–14; JOB3 §4, §7a–7c). Tests: 5 golden in `tests/audit/2026-09-04_job3/test_v7_iso01_patch_golden.py` + 2 repro on the pre-patch fixture.

**ISO-02 — fabricated balance (brother_sniper_v7, ADR-009).** `core/ic_markets.py:62` `float(r.json().get('balance', 1000.0))` never checked the status code, so the bridge's own 503 became balance 1000.0; `:63-66` returned the env `ACCOUNT_BALANCE` (6000.0, set explicitly in the box `.env`) on any exception; `bot.py:839-840` and `:1384-1385` did the same; the number passed the fail-closed margin gate and reached the equity guard and lot sizing. Fix: v7 `4937532` — `get_balance()` returns `None` on non-200 / no field / exception (`ACCOUNT_BALANCE` dead); `EquityGuard.check(None)` blocks before `update_balance`; `update_balance(None)` is a no-op; `status_summary(None)` is UNKNOWN; bot `/health` degraded + 503 with `balance_state`; `/recalibrate` refuses; startup Telegram says UNKNOWN. Tests: `tests/audit/2026-09-05_iso02/` (10 fixtures, W2-02 pin), `test_v7_fabricated_balance.py`, `test_golden_fabrication_lines_are_gone`. Status: **watch**; resolved when `/health` on the box shows `balance_state` with the bridge stopped for 10 seconds.

**ISO-03 — v7 `/execute` ignores account, no magic (brother_sniper_v7, ADR-004).** `sniper_executor.py:178-199` read secret/symbol/direction/lot/sl/tp/signal_id only; `:234-245` the MT5 request had no `magic`; `core/ic_markets.py:142-150` the bot never sent an account. Fix: v7 `f400347` — `/execute` requires `account_id == V7_MT5_LOGIN` (400 `no_account_id` / 403 `account_mismatch`); every order carries `magic V7_MAGIC` (env `V7_MAGIC_NUMBER`, default 70007); `/positions` rows carry `magic` + `ours`; the bot sends `account_id` from `V7_MT5_LOGIN`. ISO-04 (no magic) is folded into this record. Status: **watch**; resolved when the bridge log shows `magic=70007 account=52834417` on the first order.

**ISO-05 — v7 `/close` and `/modify` act on any ticket (brother_sniper_v7, ADR-004).** `sniper_executor.py:299-323` and `:372-386` resolved the ticket with `positions_get(ticket=…)` and sent the order with no magic, comment or account check; the bot's SLOT-RECON (`bot.py:400-424`) adopted any `BS_`-commented position. Reproduced against a fake terminal holding a magic-180000 (v18) position. Fix: v7 `f400347` — `_is_ours` (magic 70007 or legacy `BS_` comment) else 403 `not_ours`; nothing is sent for a foreign position. Status: **watch**.

**ISO-09 — v18 bridge never asserts its login (brother-brain-v2, ADR-004).** `executor_ic_markets/src/ic_markets/mt5_bridge.py:42-53` attached without login when any of `MT5_LOGIN`/PASSWORD/SERVER was empty (a mode `.env.example:9-10` documented); `:64-71` logged `info.login` and set `connected=True` with no comparison. Fix: brain `db74e85` — all three env values required (missing ⇒ refuse, CRITICAL); attached login asserted after `initialize()` and on every probe; mismatch ⇒ `mt5.shutdown()` + `connected False`; `asserted_login` and `trade_mode` stored; `/health` reports `account_login`, `trade_mode`, `trade_mode_name`. Box: `MT5_LOGIN=52901228` is set (JOB3 §12), so the gate condition is met. Tests: 8 bridge_identity + 5 in `tests/audit/2026-09-05_iso09` + cross-arm `test_golden_ISO09_patched_v18_bridge_refuses_without_or_against_its_login`. Status: **watch**; resolved when `/health` on the box shows `account_login 52901228` and `trade_mode 0`.

**ISO-10 — signed envelope carries no account (brother-brain-v2, ADR-004).** `shared/src/protocol/envelope.py:40-49` signed version/issued_at/nonce/target/signal with `target` a Literal `'polymarket'|'ic_markets'`; `executor_ic_markets/src/main.py:371-373` accepted on target alone and never read an account; `brain/src/signals/dispatcher.py:19-28` routed by kind with one URL per kind; `brain/src/main.py:109` hard-coded the target. One signature was valid for every IC Markets executor trusting the brain key; an unknown `account_id` inside the signal was executed anyway (reproduced through the real `/signal` route). Fix: brain `4b8352b` — `SignalEnvelope.account_id` inside the signed bytes; the dispatcher fills it from `EXECUTOR_IC_MARKETS_ACCOUNT` and refuses live dispatch without it; the executor rejects missing (`no_account_id`) or mismatched (`account_mismatch` vs the ISO-09 asserted login) envelopes after signature verification. Deploy: brain + executor together, `EXECUTOR_IC_MARKETS_ACCOUNT=52901228` in brain `.env` (not yet set). Status: **watch**; resolved when executor events show `signal_verified` with `account_id` and zero `account_mismatch` for the first live day.

**ISO-12 — daily loss dropped on unreadable balance (brother-brain-v2, ADR-009).** `executor_ic_markets/src/main.py:168` `bal = … or 0.0` and `:171` `bal = 0.0` on exception; `src/utils/state.py:67` `if balance and balance > 0` skipped the accumulation, so `MAX_DAILY_LOSS_PCT` could not trip on that loss — UNKNOWN became "no loss". Fix: brain `3b0d4a0` — `state.add_realized_pnl` parks the loss in `pending_pnl_money` (persisted) when balance is None/0 and applies it on the next measured balance; `_on_trade_closed` passes None and journals `pnl_unknown_pending`; the OPEN gate applies pending first and rejects `pnl_unknown_pending` while unreadable, tripping the cap when the applied loss crosses it. Failure injection: pending loss survives restart and still blocks. Status: **watch**; resolved when `state.json` on the box carries `pending_pnl_money` after the restart.

**ISO-14 — invalidated LIMIT converted to MARKET (brother-brain-v2, ADR-006).** `mt5_bridge.py:377-386` `[PEND->MKT]` branch set `action=TRADE_ACTION_DEAL` and `price=_mkt` when `_drift <= 0.30 * _risk`. Fix: brain `cfe18b1` — the branch is deleted; an invalid pending price is skipped with `pending_price_stale` (with `drift`) at any drift; valid pendings and MARKET orders unchanged. Met as a Job 4 finding during Job 3. Status: **watch**.

**ISO-16 — no global emergency stop across arms (both repos, ADR-008).** v18's `kill_switch` was a per-`ExecutorState` file tripped by `/admin/halt` (`state.py:19`, `main.py:109-116`); v7 had only `state['paused']` (`bot.py:818-821`) and no halt route in `sniper_executor.py`; the only cross-cutting file was `GUARDS_DISABLED`, a bypass. With v18 halted the v7 bridge still filled (cross-arm repro). Fix: brain `f93a3fd` (`executor_ic_markets/src/utils/global_stop.py`; `main.py` rejects every OPEN unless CLEAR, outside the GUARDS_DISABLED bypass; `_on_admin_halt` engages the witness) + v7 `f8aaf5f` (`sniper_executor.py` reads the same file before every `/execute`; `/admin/halt` and `/admin/status` with the `halt_admin` token contract; `/health` reports it). One file, `GLOBAL_STOP_FILE` (default `C:\brotherbot\GLOBAL_STOP`): present = STOP, unreadable = STOP; close is never blocked. Tests: v18 3 + v7 6 + cross-arm 3. Status: **watch**; resolved when `/admin/halt` on one service is followed by a 503 `global_stop` from the other on the box.

**ISO-19 — the council dispatches LLM-computed execution values (brother-brain-v2, ADR-005).** On the AI-on path (`brain/AI_ENABLED` is tracked in the repo, so it is the default, and it is PRESENT on the box), the OPEN payload that was Ed25519-signed and sent to MT5 was the ExecutorPrep LLM's own JSON: `entry_price`, `stop_loss`, `take_profit`, `risk_pct`, `order_type` (`brain/src/agents/executor_prep.py:38-44`, `risk_manager.py:31-33`, `council.py:244-262`); the only guard was a range check (entry within 5%, SL/TP within 20%, risk in (0,2]); `compute_sltp` was never called on this path. The fixture showed a 0.5-dollar GOLD stop accepted where the floor is 4.32. Fix: brain `21f6f31` — `council.build_execution_payload()` computes entry/SL/TP via `compute_sltp` with bounded model params (`_SLTP_BOUNDS`); `risk_pct` = 0.5 base, lowered-only by the model; MARKET only; calculator error rejects; ExecutorPrep retired. Replay: 12 real journal rows, 7/12 stops widened = BEHAVIOR CHANGED, unsoaked. Tests: 17 in `brain/tests/audit/2026-09-05_iso19/` + 7 in `2026-09-05_job9/`. Status: **watch**; resolved when the first `council_live` journal row shows `trace.sltp`.

**ISO-24 — an LLM vote reverses a rule block in v7 (brother_sniper_v7, ADR-005).** `filters/ai_filter.py:106-119`: when the rules blocked a non-news signal, `deepseek_tiebreak()` was asked and `take=True` with confidence ≥ 60 set `passed=True` ("AI OVERRIDE"); `filters/deepseek_vote.py:221` makes a live HTTP call to the provider. Measured on the box 2026-09-05: both API keys set, `EYE_MODEL=shadow` (which asks two providers and RETURNS the primary vote — not shadow-only), filter threshold 38 with 207 samples, so the override was ARMED; 0 `AI OVERRIDE` lines in the retained logs (retention UNKNOWN). Fix: v7 `1443ec8` — override branch removed; the vote is recorded `shadow_only: True` in `breakdown['deepseek']`; the block always stands. Tests: 5 in `tests/audit/2026-09-05_job9/test_job9_llm_override.py`. Status: **watch**; resolved when a journaled block shows "model vote recorded, not applied".

# The ten Job 3 invariants, before and after

`docs/JOB3_ISOLATION_2026-09-04.md` §1 (2026-09-04, code at c1618f5/cfc52e9) and `docs/P0_RELEASE_REPORT_2026-09-05.md` §4 (re-graded after the repo fixes):
1. A cannot execute on B — was FAIL on both arms (ISO-01, ISO-09); now PASS (v7 on box, v18 in repo).
2. B cannot execute on A — symmetric; same.
3. Same signal + same account = one execution — PASS at the bot (`symbol:signal_id`), **FAIL at the bridge, no idempotency (ISO-06, P1, still open)**; PASS per process on v18 (`signal_id` NonceStore).
4. Same signal on two accounts = two executions — PASS by construction (two processes); dedupe keys omit the account (ISO-08 v7, ISO-13 v18, P1 open).
5. Global kill blocks both — was FAIL on both; now PASS in repo (ISO-16).
6. A daily loss blocks A only — PASS per process (ISO-07 P1: `EquityState` has no account field); v18 FAIL on unreadable balance fixed (ISO-12).
7. B stays operational — PASS (per process).
8. Unknown account = NO EXECUTION — was FAIL on both (ISO-03, ISO-10); now PASS in repo.
9. Missing margin = NO EXECUTION — v7 was FAIL (ISO-02), now PASS in repo; v18 PASS on balance, **FAIL on the margin floor (ISO-11, P1, fail-open, still open)**.
10. Invalid signature = NO EXECUTION — PASS on both (v7 shared secret 403; v18 401 on tamper, 409 on replay).
Box grade for rows 1, 2, 5, 6, 8, 9: UNKNOWN until deployed.

# The other findings (P1, P2, coordination)

P1, test docstrings only, no memory record, all open: ISO-06 bridge idempotency (same `signal_id` twice = two fills); ISO-07 `EquityState` has no account field, default `peak_balance` 1000.0 (`risk/equity_guard.py:18-22`); ISO-08 bot dedupe key `symbol:signal_id` (`bot.py:284-288`); ISO-11 margin floor fail-open (`mt5_bridge.py:318-334`); ISO-13 `ExecutorState` has no account field (`state.py:14-25`); ISO-15 `GUARDS_DISABLED` file bypasses both daily caps (`main.py:65-68, 421-430`; the ISO-16 global stop is NOT bypassable by it); ISO-20 executor `s.get("risk_pct", 0.5)` sizes a missing risk at the cap (`main.py:433`); ISO-21 `pine_trust.py:73-78` calculator exception falls back to raw Pine SL/TP (fail-open).
P2: ISO-17 two `terminal64.exe` processes from `C:\MT5_v18` — RESOLVED, not a fault (one install run by two Windows users: the RDP session and the session-0 service that launched its own instance 7 seconds after the 2026-09-01 deploy); ISO-18 the box `MAGIC_NUMBER` is 20260530 while code default, `.env.example:20` and `docs/PROTOCOL.md:97` all say 180000 — documentation drift, a redeploy from `.env.example` would orphan every open position to the reconciler; ISO-22 orphan adoption with `sl or 0` then "managed" on 0 (`bot.py:411-415`); ISO-23 `magic_number: 180000` hard-coded in the brain payload while the executor ignores it (`pine_trust.py:99`, `executor_prep.py:43`); ISO-25 filter threshold default 5 — closed as measured (box threshold 38); the `USE_DEMO=false` label in the v7 `.env` while the constitution says all demo (label only, `bot.py:34` dead XTB port).
Coordination: W2-01 (P1) Window 2 branched from `main`, which never contained the Job 3 audit branch, so its v7 tree was pre-ISO-01 again — closed by two zero-conflict fast-forwards (JOB3 §10–§11, ledger row 23 `converged`); W2-02 (P2) the ISO-02 proposal's `GuardResult(...)` hunk omitted four required fields and would raise `TypeError` — closed on v7 `5369fa3` using `_block(...)` before `update_balance` (ledger row 22).
Accepted risk, recorded: the brain's fail-soft path (`FAILSOFT_MAX_PER_DAY=2`, A/A+ approved from Pine on agent failure) bypasses the council by explicit `.env` decision — a tension with ADR-003 for the real-money gate (JOB8_9 §2).

# Box measurements (what is KNOWN and what is still UNKNOWN)

Measured on Contabo 2026-09-04T21:53Z (JOB3 §6, ledger row 9): `BRAIN_DISPATCH_MODE=live`; `EXECUTOR_IC_MARKETS_URL` ends in `:8080`; `EXECUTOR_URL` ends in `:5001/execute`; `FAILSOFT_MAX_PER_DAY=2`; `ACCOUNT_BALANCE=6000.0` set explicitly; `USE_DEMO=false`; v7 `/health` account 52834417; v18 `/health` `dry_run false`, `kill_switch false`, a different balance at the same second (so not the same account). Measured on Windows 2026-09-05 (JOB3 §12, ledger row 24): `DRY_RUN=false`, `MT5_LOGIN=52901228`, `MT5_PATH=C:\MT5_v18\terminal64.exe`, `MAGIC_NUMBER=20260530`, `GUARDS_DISABLED` absent, `ADMIN_HALT_TOKEN` set, NSSM `SniperExecutorV7` env exactly `V7_MT5_LOGIN`; 52901228 is a DEMO account (terminal title `ICMarketsEU-Demo: Demo Account - Hedge`). Measured on Contabo 2026-09-05 (JOB8_9 §6, ledger row 28): `brain/AI_ENABLED` PRESENT; DeepSeek and Gemini keys set; `EYE_MODEL=shadow`; `learning/weights.json` threshold 38, 207 samples; 0 `AI OVERRIDE` in retained logs; Windows logs are daily zips 08-06..09-04 plus 09-05 plain, no `service_stderr.log`.
Still UNKNOWN at the last commit: the v18 executor's own "MT5 connected: account=" log line (it is in the zipped 2026-09-01 day; JOB8_9 §7 gives the `Expand-Archive` command); the `trade_mode` of 52834417; every fix's behaviour on the boxes until deployed; the `council_live` row count (the first grep used `": "` but the journal is written compact).

# The 296-test green statement

`docs/P0_RELEASE_REPORT_2026-09-05.md` §2, run in clean worktrees at the branch heads on 2026-09-05: `brother_sniper_v7` `python3 -m pytest -q` 183 passed; `brother-brain-v2` `python3 -m pytest tests/audit executor_ic_markets/tests -q` 45 passed; `brother-brain-v2/brain` `python3 -m pytest tests -q` 53 passed; `brother-developer` `python3 -m pytest -q` with siblings present 15 passed; total 296 passed, 0 failed (ledger row 39 `tests.total: 296`). Pre-existing suites are inside those numbers (v7 140 before the audit, brain 25, executor 4, developer 11). Every `test_repro_*` still runs against its pre-fix fixture copy, so each finding stays reproducible after the fix. Earlier checkpoints: JOB3 §1 v7 14 / brain 18 / developer 4 audit tests; §13 on `main` v7 157, brain 27+25, developer 13.

# The GO / NO-GO verdicts

Job 3 closure (JOB3 §13, 2026-09-05, ledger row 25): deliverables COMPLETE, isolation NOT ACHIEVED (1 of 9 P0 resolved); DUAL-DEMO GO with conditions; DUAL-SHADOW GO; REAL MONEY NO-GO.
Final (P0 report §10, ledger row 39): DUAL-SHADOW GO (nothing changes shadow paths; it is where the ISO-19 7/12 change must be soaked); DUAL-DEMO GO only after the coupled deploys in §9 and after each `/health` witness is read; REAL MONEY tiny test NO-GO for five reasons — (1) zero of the ten repo fixes is verified on a box; (2) ISO-19 is BEHAVIOR CHANGED on 7/12 real rows with no soak and n<20 is luck; (3) money-touching P1s remain open (ISO-06, ISO-11, ISO-15); (4) 52834417 `trade_mode` unmeasured and the v18 connect line unread; (5) the window is not the gate. Real money is reconsidered only when every box column reads a measured witness, the ISO-19 soak has n≥20 shadow rows reviewed by a human, ISO-06/11/15 are fixed and deployed, and the two UNKNOWN box facts are measured.

# Exact commits (P0 report §8)

brother_sniper_v7 (main `a3640d6` → head `f8aaf5f`): `f8aaf5f` ISO-16 v7 side; `f400347` ISO-03 + ISO-05; `eef7c1b` FakeMT5 gains `shutdown()` and `honour_login`; `4937532` ISO-02; `1443ec8` ISO-24. brother-brain-v2 (main `4c2afcf` → head `f93a3fd`): `f93a3fd` ISO-16 v18 side; `cfe18b1` ISO-14; `3b0d4a0` ISO-12; `4b8352b` ISO-10; `db74e85` ISO-09; `21f6f31` ISO-19. brother-developer: `ddb87b4` ISO-19, `9978ee3` ISO-24, `1a827a0` ISO-02, `0ba9e98` ISO-09, `631a5d5` ISO-10, `049aa71` ISO-03+05, `befb4b9` ISO-12, `d1b6167` ISO-14, `5a061f0` ISO-16, `55aba5b` final report. All of the v7 and brain commits above are present in the sibling checkouts as of 2026-09-16 with 2026-09-05 dates; the ISO-01 observation commit `c1618f5` is dated 2026-09-02 and the brain observation commit `cfc52e9` 2026-09-03. Of the three seeded platform records, `7fa7330` (trade twins, "v5.23 — one broker ticket, one row", 2026-09-03) is present in the local Sniper-System checkout; `5260ce9` and `297554c` are not reachable there (UNKNOWN whether rewritten or on another branch).
